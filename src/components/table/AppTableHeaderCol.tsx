import styled from "styled-components";

const AppTableHeaderCol = styled.div<{ width?: string }>`
    text-align: left;
    color: rgb(99, 123, 159);
    font-size: 12px;
    justify-content: center;
`;

export default AppTableHeaderCol;
